# Dependency overrides for the test environment (uv `--overrides`).
#
# Home Assistant pins `lru-dict==1.3.0`, whose last release predates CPython
# 3.13 and ships no cp313 wheel — so installing it on the Python 3.13
# toolchain means compiling the C extension, and the toolchain carries no C
# compiler. lru-dict 1.4.1 is the same library with 3.13/3.14 wheels added.
#
# Drop this file once Home Assistant's own pin moves past 1.3.0.
lru-dict==1.4.1
